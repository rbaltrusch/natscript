define function test as {
    set a to 23 and multiply it by 2 and print it
    define function test2 as { set b to 24 and print it } and call it
}
call test

set a to {print 1} and call it

define function ab as {print 1} and call it
define function abc as {print 2} and call it
define function f expecting [a b] as {add a to b and print it} and call f with [1, 2]
